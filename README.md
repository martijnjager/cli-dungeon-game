# CLI Dungeon Game

A simple turn-based dungeon exploration CLI game written in PHP.

## Usage

- Run locally: composer install && composer start
- Run in Docker: make build && make run
- Run tests: composer test or make test

Commands inside the game: move <north|south|east|west>, attack, look, status, quit
